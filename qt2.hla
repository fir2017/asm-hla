program qt2;
#include("stdlib.hhf");
static
    num8        :int8[5]   := [1,2,3,4,5];


begin qt2;
   mov('a', eax);
   stdout.put(eax, nl);
   cmp(eax, 97);
   jne EXITNOW;
    stdout.put("[equal]", nl);

    EXITNOW:
    stdout.put("[exiting...]", nl);
end qt2;
